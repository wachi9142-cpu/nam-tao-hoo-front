# Public URL of the site, used for share previews (Open Graph) and sitemap
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Mock admin passcode for /admin (dev only — real admin auth comes from the backend)
NEXT_PUBLIC_ADMIN_CODE=1234
